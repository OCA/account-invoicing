To configure this module, you need to:

1. Create utility types (for example, water, electricity, or other utilities) via menu
   Utility > Configuration > Utility Type
2. Define utilities linked to products, such as a meter for Product A, via menu
   Utility > Configuration > Utility
3. Configure the default accounting journal by going to menu Settings > Invoicing >
   Account Utility > Account Utility Journal
