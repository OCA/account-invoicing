<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <app name="account" position="inside">
                <block title="Account Utility" name="account_utility_journal_container">
                    <setting
                        id="journal_account_utility"
                        string="Account Utility Journal"
                        help="Record for the Default Journal of a Account Utility."
                    >
                        <field
                            name="journal_account_utility_id"
                            domain="[('type', '=', 'sale')]"
                        />
                    </setting>
                    <setting
                        id="utility_rounding_method"
                        company_dependent="1"
                        string="Rounding Method"
                        help="How total the utility is computed in utility invoicing"
                        title="Round per Line is advised when the printed line amounts must add up exactly to the document total."
                    >
                        <field
                            name="utility_rounding_method"
                            class="o_light_label mt16"
                            widget="radio"
                        />
                    </setting>
                </block>
            </app>
        </field>
    </record>
</odoo>
