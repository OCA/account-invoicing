- To configure the OCR debugging behavior by enabling the Log OCR Entities as JSON (Debug) option in the Invoicing settings.

- When activated, the system attaches a JSON file containing all OCR-extracted entities to the chatter on the Invoice form for debugging purposes.