Once the module is installed, no additional configuration is required.

When a sale order has a payment transaction in *Done* state that covers
the full order amount, all order lines become invoiceable regardless of
the product invoice policy. You can then create the invoice directly
from the sale order without needing to validate a delivery first.
