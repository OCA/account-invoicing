Usage
=====
#. To use this module, the module account must be installed
the module adds a field supplier_invoice_number to vendor bills, which updates the vendor reference
this field checks if it's unique for the vendor and displays a warning if it's not unique.