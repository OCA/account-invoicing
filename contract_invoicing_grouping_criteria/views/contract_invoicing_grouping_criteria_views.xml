<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_contract_invoice_grouping_criteria_form" model="ir.ui.view">
        <field name="model">contract.invoicing.grouping.criteria</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <h1>
                        <field name="name" />
                    </h1>
                    <group>
                        <field name="field_ids" widget="many2many_tags" />
                        <field name="line_field_ids" widget="many2many_tags" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="view_contract_invoice_grouping_criteria_tree" model="ir.ui.view">
        <field name="model">contract.invoicing.grouping.criteria</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="field_ids" widget="many2many_tags" />
                <field name="line_field_ids" widget="many2many_tags" />
            </tree>
        </field>
    </record>
    <record id="view_contract_invoice_grouping_criteria_search" model="ir.ui.view">
        <field name="model">contract.invoicing.grouping.criteria</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
            </search>
        </field>
    </record>
    <record
        model="ir.actions.act_window"
        id="action_contract_invoice_grouping_criteria"
    >
        <field name="name">Contracts Invoicing Grouping Criteria</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">contract.invoicing.grouping.criteria</field>
        <field name="view_mode">tree,form</field>
    </record>
    <menuitem
        id="menu_contract_invoice_grouping_criteria"
        action="action_contract_invoice_grouping_criteria"
        name="Contracts Invoicing Grouping Criteria"
        parent="account.account_management_menu"
        sequence="101"
    />
</odoo>
