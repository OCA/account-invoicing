This module adds a **re-import** action on invoices, allowing users to 
**rebuild invoice lines from an existing attachment**.

This is useful in cases where an invoice has been created from a document
(PDF, XML, etc.), and the initial import result needs to be
**corrected or re-executed** without re-uploading the file or recreating the
invoice manually.
