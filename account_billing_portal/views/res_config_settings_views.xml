<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_invoice_config_settings" model="ir.ui.view">
        <field name="name">res.config.settings</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='show_sale_receipts']" position="after">
                <setting
                    id="billing_email_template_id"
                    company_dependent="1"
                    string="Choose Billing Email Template"
                    help="Select the email template used for billing emails. If left empty, the default template provided by Account Billing Portal will be used."
                >
                    <field name="billing_email_template_id" />
                </setting>
                <setting
                    id="billing_portal_report"
                    company_dependent="1"
                    string="Choose Billing Portal Report"
                    help="If set, this report will be used for both the billing portal and the billing email attachment sent to the partner. If left empty, the standard report from the Account Billing module will be used instead."
                >
                    <field name="billing_portal_report" />
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
