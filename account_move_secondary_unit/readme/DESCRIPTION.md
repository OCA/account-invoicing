This module extends account moves to support a secondary unit of measure from a
different UoM category on invoice and bill lines. It adds secondary quantity, secondary
unit, and secondary price columns, where changing the secondary quantity or price
automatically recalculates the corresponding primary values and vise versa.
The printed invoice report can be configured to show primary, secondary, or both units
for quantities and prices.
