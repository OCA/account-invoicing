<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Oriol Gracia MTS <o.gracia@madetosoft.com>
     Copyright 2026 Juan Arcos MTS <j.arcos@madetosoft.com>
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="purchase_order_view_form" model="ir.ui.view">
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='button_approve']" position="after">
                <button
                    name="action_create_invoice"
                    string="Create Bill"
                    type="object"
                    class="oe_highlight"
                    context="{'create_bill':True}"
                    invisible="state != 'purchase' or invoice_status in ('no', 'invoiced')"
                    data-hotkey="w"
                />
            </xpath>
            <xpath expr="//header/button[@name='action_rfq_send']" position="after">
                <button
                    name="action_create_invoice"
                    string="Create Bill"
                    type="object"
                    context="{'create_bill':True}"
                    invisible="state != 'purchase' or invoice_status not in ('no', 'invoiced') or not order_line"
                    data-hotkey="w"
                />
            </xpath>
        </field>
    </record>
</odoo>
