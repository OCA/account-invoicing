This module allows to validate supplier invoices that have the same reference number.
Leasing invoices, for example, which are the same each month and whose ISR number does not change.

This module deactivates the odoo check for unique reference on supplier invoice reference.
