This module disable the automatic matching of products based on their name
when importing EDI invoices, ensuring products are only matched using explicit
identifiers (such as vendor references, barcodes, or internal codes)