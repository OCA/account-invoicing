<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_config_settings" model="ir.ui.view">
        <field name="name">res.config.settings</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@id='proforma_configuration']" position="after">
                <div class="col-12 col-lg-6 o_setting_box">
                    <div class="o_setting_right_pane">
                        <label for="picking_date_field_for_invoice_date" />
                        <div class="text-muted">
                            Select the date or datetime field from the picking.
                            If set, the value of this field or the effective date of the last completed picking associated with the sales order will be used as the invoice date.
                        </div>
                        <div class="mt8">
                            <field
                                name="picking_date_field_for_invoice_date"
                                class="o_light_label"
                            />
                        </div>
                    </div>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
